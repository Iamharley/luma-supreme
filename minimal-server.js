const express = require("express"); const app = express(); const PORT = process.env.PORT || 3000; app.get("/", (req, res) => { res.send("�� LUMA MINIMAL - OK"); }); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "LUMA Minimal Server", timestamp: new Date().toISOString(), port: PORT }); }); app.listen(PORT, "0.0.0.0", () => { console.log("🚀 LUMA Minimal Server démarré"); console.log(`🌐 Port: ${PORT}`); console.log("✅ Health check: /health"); console.log("✅ Ready for Railway"); console.log("✅ Environment:", process.env.NODE_ENV); console.log("✅ Process ID:", process.pid); });
