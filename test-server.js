const express = require("express"); const app = express(); const PORT = process.env.PORT || 3000; const HOST = process.env.HOST || "0.0.0.0"; app.get("/health", (req, res) => { res.json({ status: "healthy", service: "LUMA Test Server", timestamp: new Date().toISOString(), port: PORT, host: HOST }); }); app.get("/", (req, res) => { res.send("🔥 LUMA TEST SERVER - OK"); }); app.listen(PORT, HOST, () => { console.log("🚀 LUMA Test Server démarré"); console.log(`🌐 Serveur sur ${HOST}:${PORT}`); console.log("✅ Health check: /health"); });
